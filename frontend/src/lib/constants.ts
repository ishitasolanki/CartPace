/** Mirrors sim/scenarios.py's N_STRATA. Fixed at configuration time and
 * never learned (project.md section 3.2) -- there is no endpoint for it
 * because it never changes, so a constant here is honest, not a shortcut. */
export const N_STRATA = 6;
